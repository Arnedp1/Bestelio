# PostgreSQL (docker-compose default)
DATABASE_URL="postgresql://honger:honger@localhost:5433/honger?schema=public"

# Auth — generate: openssl rand -base64 32
AUTH_SECRET="change-me-in-production"
NEXTAUTH_SECRET="change-me-in-production"
# Moet overeenkomen met je browser-URL (gebruik localhost, niet 127.0.0.1)
NEXTAUTH_URL="http://localhost:3000"

# Demo tenant hostname + admin (seed)
DEFAULT_TENANT_HOST="localhost"
SEED_ADMIN_EMAIL="admin@demo.local"
SEED_ADMIN_PASSWORD="demo-admin-change-me"

# --- Betalingen ---
# Echte Mollie: plak test_ key in Admin → Instellingen (aanbevolen) of hier:
# MOLLIE_API_KEY="test_…"
PAYMENT_SIMULATE="false"

# Demo zonder Mollie (geen betaalscherm):
# PAYMENT_SIMULATE="true"

# --- Keukenbon / bonprinter (ESC/POS) ---
# Genereer: openssl rand -base64 32
# PRINT_AGENT_SECRET="change-me-print-agent-secret"
# PRINT_AGENT_API_URL="http://localhost:3000"
# PRINTER_NAME="EPSON TM-T20"   # exacte naam in Windows Printers
# PRINT_AGENT_POLL_MS="3000"

# --- QZ Tray signing (voor "Remember this decision") ---
# Plaats PEM als 1 regel met \n escapes
# QZ_TRAY_CERT_PEM="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"
# QZ_TRAY_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
