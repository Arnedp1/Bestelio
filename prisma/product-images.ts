/**
 * Geen demo-foto's: leeg imageUrl → storefront toont emoji-placeholder (🍽️).
 * Productie: eigen foto's per product via admin of object storage.
 */
export function imageUrlForProduct(): null {
  return null;
}
